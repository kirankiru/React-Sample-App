import { SAMPLE_REACT_APP } from '../../utils/constants';

const Header = () => {
  return (
    <header className="App-header app-header-post">
      <p className="text-color">
        { SAMPLE_REACT_APP }
      </p>
    </header>
  );
};

export default Header;
